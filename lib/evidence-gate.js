(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.HuntIQEvidenceGate=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){
'use strict';
const clamp=(n,min,max)=>Math.min(max,Math.max(min,Number(n)||0));
function evaluateEvidence({history={},anomaly={},resale={},economics={},deal={},liquidity={},sourceReliability={}}={}){
  const samples=Math.max(0,Number(history.sampleCount||history.samples||0));const spanDays=Math.max(0,Number(history.spanDays||0));const historyConfidence=clamp(history.confidence!=null?history.confidence:(samples>=12&&spanDays>=30?90:samples>=6&&spanDays>=14?72:samples>=3?50:25),0,100);const historyCoverageScore=clamp(history.historyCoverageScore==null?100:history.historyCoverageScore,0,100);const maxHistoryGapDays=Math.max(0,Number(history.maxGapDays)||0);const anomalyConfidence=clamp(anomaly.confidence!=null?anomaly.confidence:anomaly.score,0,100);const anomalyPhase=anomaly.phase||anomaly.lifecycle||'unknown';const resaleConfidence=clamp(resale.confidence!=null?resale.confidence:resale.resaleConfidence,0,100);const resaleFreshnessScore=clamp(resale.resaleFreshnessScore==null?100:resale.resaleFreshnessScore,0,100);const newestSaleAgeDays=resale.newestSaleAgeDays==null?null:Math.max(0,Number(resale.newestSaleAgeDays)||0);const medianSaleAgeDays=resale.medianSaleAgeDays==null?null:Math.max(0,Number(resale.medianSaleAgeDays)||0);const soldCount=Math.max(0,Number(resale.soldCount90||resale.soldCount30||resale.soldCount||0));const priceIntegrity=clamp(resale.priceIntegrity==null?100:resale.priceIntegrity,0,100);const resaleOutlierCount=Math.max(0,Number(resale.outlierCount)||0);
  const rawRiskAdjustedRoi=Number(economics.riskAdjustedRoi)||0;const rawRiskAdjustedProfit=Number(economics.riskAdjustedProfit)||0;const riskAdjustedRoi=economics.sourceAdjustedRoi==null?rawRiskAdjustedRoi:Number(economics.sourceAdjustedRoi)||0;const riskAdjustedProfit=economics.sourceAdjustedProfit==null?rawRiskAdjustedProfit:Number(economics.sourceAdjustedProfit)||0;const downsideRoi=economics.sourceAdjustedDownsideRoi==null?(economics.downsideRoi==null?riskAdjustedRoi:Number(economics.downsideRoi)||0):Number(economics.sourceAdjustedDownsideRoi)||0;const confidenceAdjustedRoi=economics.sourceAdjustedConfidenceAdjustedRoi==null?(economics.confidenceAdjustedRoi==null?riskAdjustedRoi:Number(economics.confidenceAdjustedRoi)||0):Number(economics.sourceAdjustedConfidenceAdjustedRoi)||0;const decisionFloorRoi=economics.decisionFloorRoi==null?Math.min(riskAdjustedRoi,downsideRoi,confidenceAdjustedRoi):Number(economics.decisionFloorRoi)||0;const decisionFloorProfit=economics.decisionFloorProfit==null?Math.min(riskAdjustedProfit,Number(economics.downsideProfit)||riskAdjustedProfit,Number(economics.confidenceAdjustedProfit)||riskAdjustedProfit):Number(economics.decisionFloorProfit)||0;const headroom=Number(economics.target50Headroom!=null?economics.target50Headroom:economics.acquisitionHeadroom)||0;const expectedFutureCredit=Math.max(0,Number(economics.expectedFutureCredit)||0);const cashOnlyRiskAdjustedProfit=riskAdjustedProfit-expectedFutureCredit;const deferredCreditDependent=expectedFutureCredit>0&&cashOnlyRiskAdjustedProfit<=0;const acquisition=economics.acquisition||{};const promotionStatus=acquisition.promotionStatus||'eligible';const promotionReasons=Array.isArray(acquisition.promotionReasons)?acquisition.promotionReasons:[];const requestedPromotionValue=Math.max(0,Number(acquisition.requestedInstantDiscount)||0)+Math.max(0,Number(acquisition.requestedCheckoutCredit)||0)+Math.max(0,Number(acquisition.requestedFutureCredit)||0);const promotionConfigured=Boolean(acquisition.promotion||acquisition.basketPromotion||acquisition.multiBuyPromotion);const promotionUnconfirmed=(requestedPromotionValue>0||promotionConfigured)&&promotionStatus!=='eligible';
  const retailerSourceScore=sourceReliability.retailer&&sourceReliability.retailer.score!=null?clamp(sourceReliability.retailer.score,0,100):null;const resaleSourceScore=sourceReliability.resale&&sourceReliability.resale.score!=null?clamp(sourceReliability.resale.score,0,100):null;const combinedSourceScore=sourceReliability.combined&&sourceReliability.combined.score!=null?clamp(sourceReliability.combined.score,0,100):(retailerSourceScore!=null&&resaleSourceScore!=null?Math.min(retailerSourceScore,resaleSourceScore):retailerSourceScore!=null?retailerSourceScore:resaleSourceScore);
  const liquidityScore=liquidity.liquidityScore==null?null:clamp(liquidity.liquidityScore,0,100);const capitalEfficiencyScore=liquidity.capitalEfficiencyScore==null?null:clamp(liquidity.capitalEfficiencyScore,0,100);const liquidityBand=liquidity.liquidityBand||null;const daysToSell=liquidity.daysToSell==null?null:Math.max(0,Number(liquidity.daysToSell)||0);const fresh=deal.fresh!==false&&deal.stale!==true;const verified=Boolean(deal.verified||deal.confirmed);const blockers=[];const warnings=[];
  if(!fresh)blockers.push('stale-price');if(samples<2)blockers.push('insufficient-price-history');else if(samples<6||spanDays<14||historyConfidence<45)warnings.push('thin-price-history');if(historyConfidence<30)blockers.push('weak-history-baseline');if(historyCoverageScore<35)blockers.push('poor-price-history-coverage');else if(historyCoverageScore<70)warnings.push('gappy-price-history');
  if(anomalyConfidence<45)blockers.push('weak-anomaly-evidence');else if(anomalyConfidence<70)warnings.push('moderate-anomaly-evidence');if(resaleConfidence<45||soldCount<2)blockers.push('weak-resale-evidence');else if(resaleConfidence<70||soldCount<5)warnings.push('moderate-resale-evidence');if(resaleFreshnessScore<30)blockers.push('stale-resale-evidence');else if(resaleFreshnessScore<60)warnings.push('aging-resale-evidence');if(priceIntegrity<70)blockers.push('weak-resale-price-integrity');else if(priceIntegrity<90||resaleOutlierCount>0)warnings.push('resale-price-outliers-filtered');
  if(retailerSourceScore!=null){if(retailerSourceScore<45)blockers.push('weak-retailer-source-reliability');else if(retailerSourceScore<70)warnings.push('mixed-retailer-source-reliability');}if(resaleSourceScore!=null){if(resaleSourceScore<45)blockers.push('weak-resale-source-reliability');else if(resaleSourceScore<70)warnings.push('mixed-resale-source-reliability');}if(combinedSourceScore!=null&&combinedSourceScore<85)warnings.push('source-reliability-haircut-applied');
  if(riskAdjustedProfit<=0||riskAdjustedRoi<=0)blockers.push('negative-risk-adjusted-economics');if(downsideRoi<0)blockers.push('negative-downside-roi');if(confidenceAdjustedRoi<0)blockers.push('negative-confidence-adjusted-roi');else if(confidenceAdjustedRoi<15)warnings.push('thin-confidence-adjusted-margin');if(decisionFloorProfit<=0||decisionFloorRoi<=0)blockers.push('negative-decision-floor-economics');else if(decisionFloorRoi<15)warnings.push('thin-decision-floor-margin');if(headroom<0)warnings.push('below-50pct-roi-target');if(expectedFutureCredit>0)warnings.push('includes-deferred-retailer-credit');if(deferredCreditDependent)warnings.push('deferred-credit-dependent');if(promotionUnconfirmed)warnings.push(promotionStatus==='unknown'?'promotion-eligibility-unconfirmed':'promotion-not-applicable');if(anomalyPhase==='established-clearance'||anomalyPhase==='persistent-markdown')warnings.push('persistent-markdown');if(liquidityBand==='illiquid'&&(daysToSell==null||daysToSell>90||liquidityScore<30))blockers.push('illiquid-resale-market');else if(liquidityBand==='slow')warnings.push('slow-resale-velocity');if(capitalEfficiencyScore!=null&&capitalEfficiencyScore<25)warnings.push('low-capital-efficiency');
  let evidenceScore=historyConfidence*.18+historyCoverageScore*.05+anomalyConfidence*.23+resaleConfidence*.23+resaleFreshnessScore*.07+priceIntegrity*.05+clamp(Math.min(riskAdjustedRoi,confidenceAdjustedRoi),0,100)*.19;if(liquidityScore!=null)evidenceScore=evidenceScore*.90+liquidityScore*.10;if(combinedSourceScore!=null)evidenceScore=Math.min(evidenceScore,combinedSourceScore);evidenceScore=clamp(evidenceScore,0,100);let alertLevel='digest';if(!blockers.length&&evidenceScore>=82&&riskAdjustedRoi>=50&&confidenceAdjustedRoi>=35&&decisionFloorRoi>=20&&verified)alertLevel='instant';else if(!blockers.length&&evidenceScore>=68&&riskAdjustedRoi>=25&&confidenceAdjustedRoi>=15&&decisionFloorRoi>=5)alertLevel='standard';else if(blockers.length)alertLevel='suppressed';if(alertLevel==='instant'&&(liquidityBand==='slow'||(capitalEfficiencyScore!=null&&capitalEfficiencyScore<45)||deferredCreditDependent||promotionUnconfirmed||priceIntegrity<90||historyCoverageScore<85||resaleFreshnessScore<80||(combinedSourceScore!=null&&combinedSourceScore<85)))alertLevel='standard';const alertEligible=alertLevel==='instant'||alertLevel==='standard';const confidenceBand=evidenceScore>=82?'high':evidenceScore>=65?'medium':'low';return{evidenceScore:Math.round(evidenceScore),confidenceBand,alertEligible,alertLevel,blockers,warnings,components:{history:Math.round(historyConfidence),historyCoverageScore:Math.round(historyCoverageScore),maxHistoryGapDays:+maxHistoryGapDays.toFixed(1),anomaly:Math.round(anomalyConfidence),resale:Math.round(resaleConfidence),resaleFreshnessScore:Math.round(resaleFreshnessScore),newestSaleAgeDays,medianSaleAgeDays,priceIntegrity:Math.round(priceIntegrity),resaleOutlierCount,rawRiskAdjustedRoi:+rawRiskAdjustedRoi.toFixed(1),riskAdjustedRoi:+riskAdjustedRoi.toFixed(1),downsideRoi:+downsideRoi.toFixed(1),confidenceAdjustedRoi:+confidenceAdjustedRoi.toFixed(1),decisionFloorProfit:+decisionFloorProfit.toFixed(2),decisionFloorRoi:+decisionFloorRoi.toFixed(1),soldCount,samples,spanDays,verified,fresh,expectedFutureCredit:+expectedFutureCredit.toFixed(2),cashOnlyRiskAdjustedProfit:+cashOnlyRiskAdjustedProfit.toFixed(2),deferredCreditDependent,promotionStatus,promotionReasons,promotionConfigured,promotionUnconfirmed,requestedPromotionValue:+requestedPromotionValue.toFixed(2),retailerSourceScore:retailerSourceScore==null?null:Math.round(retailerSourceScore),resaleSourceScore:resaleSourceScore==null?null:Math.round(resaleSourceScore),combinedSourceScore:combinedSourceScore==null?null:Math.round(combinedSourceScore),liquidityScore:liquidityScore==null?null:Math.round(liquidityScore),capitalEfficiencyScore:capitalEfficiencyScore==null?null:Math.round(capitalEfficiencyScore),liquidityBand,daysToSell}};}
return{evaluateEvidence};
});