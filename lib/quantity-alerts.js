(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.HuntIQQuantityAlerts=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){
'use strict';
function gate(plan={},options={}){const minExpectedProfit=Number(options.minExpectedProfit??25);const minFulfillmentConfidence=Number(options.minFulfillmentConfidence??55);const allowStaleInventory=options.allowStaleInventory===true;const reasons=[];if((Number(plan.plannedUnits)||0)<1)reasons.push('no-buyable-units');if((Number(plan.expectedProfit)||0)<minExpectedProfit)reasons.push('lot-profit');if((Number(plan.fulfillmentConfidence)||0)<minFulfillmentConfidence)reasons.push('fulfillment-confidence');if(plan.inventoryStale===true&&!allowStaleInventory)reasons.push('stale-inventory');if(String(plan.recommendation||'').startsWith('skip'))reasons.push('purchase-plan-skip');const blocked=reasons.length>0;let priorityPenalty=0;if(!blocked){if(Number(plan.plannedUnits)===1)priorityPenalty+=5;if(Number(plan.fulfillmentConfidence)<75)priorityPenalty+=8;if(Number(plan.demandCoverage)<50)priorityPenalty+=5;if(plan.inventoryAgeHours!=null&&Number(plan.inventoryAgeHours)>4)priorityPenalty+=5;}return{blocked,reasons:[...new Set(reasons)],priorityPenalty,plannedUnits:Number(plan.plannedUnits)||0,expectedProfit:Number(plan.expectedProfit)||0,inventoryStale:plan.inventoryStale===true,inventoryAgeHours:plan.inventoryAgeHours??null};}
function fingerprint(plan={},result=gate(plan)){return['qty-alert',result.blocked?'blocked':'pass',result.plannedUnits,Math.round(result.expectedProfit),result.inventoryStale?'stale':'fresh',result.inventoryAgeHours==null?'na':Math.floor(Number(result.inventoryAgeHours)/2)*2,result.reasons.join(',')||'ok'].join(':');}
return{gate,fingerprint};
});