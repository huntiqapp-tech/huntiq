(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.HuntIQFulfillment=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){
'use strict';
const clamp=(n,min,max)=>Math.min(max,Math.max(min,Number(n)||0));
function assess(opportunity={},options={}){const inventory=opportunity.inventory||{};const quantity=Number(inventory.quantity??opportunity.inventoryQuantity);const explicit=Number(opportunity.fulfillmentConfidence??inventory.confidence);const verified=opportunity.inventoryVerified===true||inventory.verified===true;const inStock=opportunity.inStock===true||inventory.inStock===true||quantity>0;const pickup=opportunity.pickupAvailable===true||inventory.pickupAvailable===true;const sourceConfidence=Number(opportunity.evidenceQuality);let confidence=Number.isFinite(explicit)?clamp(explicit,0,100):55;if(!Number.isFinite(explicit)){if(verified)confidence+=20;if(inStock)confidence+=15;if(pickup)confidence+=5;if(Number.isFinite(sourceConfidence))confidence+=10*clamp(sourceConfidence,0,1);if(quantity===0)confidence=0;}confidence=Math.round(clamp(confidence,0,100));const status=quantity===0?'unavailable':confidence>=80?'strong':confidence>=60?'moderate':'weak';return{confidence,status,verified,inStock,quantity:Number.isFinite(quantity)?quantity:null};}
function adjustEconomics(economics={},assessment={}){const trust=clamp((Number(assessment.confidence)||0)/100,0,1);const profit=Number(economics.profit)||0;const roi=Number(economics.roi)||0;return{...economics,expectedProfit:+(profit*trust).toFixed(2),expectedRoi:+(roi*trust).toFixed(2),fulfillmentConfidence:Number(assessment.confidence)||0};}
function fingerprint(opportunity={}){const a=assess(opportunity);const bucket=Math.floor(a.confidence/10)*10;return['fulfill',a.status,bucket,a.quantity==null?'na':a.quantity].join(':');}
return{assess,adjustEconomics,fingerprint};
});